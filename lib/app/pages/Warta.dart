import 'package:flutter/material.dart';
import '../Constants.dart';

class Warta extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('WARTA PAGE', style: TextStyle(color: Constants.COLOR_PRIMARY_DARK)));
  }
}
